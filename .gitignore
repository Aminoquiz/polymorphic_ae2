.gradle/
build/
*/build/
*/out/
.idea/
*.iml
*.iws
.vscode/
run/
*/run/

# Local compile-only dependencies (AE2, Polymorph+). Not ours to redistribute,
# and the build picks them up from here on the author's machine only.
libs/*
!libs/README.md

# Mod page descriptions live at polymorphic_ae2/ root, not per-port
DESCRIPTION_modrinth.md
DESCRIPTION_curseforge.md

# Private planning docs and machine-specific overrides never belong here.
# Local paths go in <user home>/.gradle/gradle.properties instead.
ROADMAP.md
local.properties
